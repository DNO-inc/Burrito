{# !!! THIS FILE IS GENERATED BY ./scripts/get_text_from_html.sh SCRIPT, DO NOT UPDATE IT MANUALLY !!! #}
   {% extends "common/base" %} {% import "common/email_func.jinja" as func %} {% block content %}

   Шановна адміністраціє,

   Цим повідомленням нагадуємо, що наступні тікети перебувають в статусі "ACCEPTED" протягом останніх {{ func.pluralize_day(days_count) }}:
   {% for ticket_item in tickets_data %}
     * #{{ ticket_item.ticket_id }} "{{ ticket_item.subject }}"
       Дата створення: {{ ticket_item.created }}
       {% endfor %}

   Будь ласка, зверніть увагу на вище зазначені тікети, переведіть їх в статус OPEN або CLOSED.

   Дякуємо за увагу!
   {% endblock %}
