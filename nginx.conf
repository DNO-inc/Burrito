worker_processes 2;

events {
    worker_connections 1000;

    use epoll;

    multi_accept on;
}

http {
    server {
        client_header_buffer_size 1k;
        client_max_body_size 10m;
        keepalive_timeout 10;

        access_log off;

        listen 8080;

        # For SSU cabinet server
        location  / {
            proxy_pass http://burrito_meta:10006/meta/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }

        location  /about/ {
            proxy_pass http://burrito_about:10000/about/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /admin/ {
            proxy_pass http://burrito_admin:10001/admin/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /anon/ {
            proxy_pass http://burrito_anon:10002/anon/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /auth/ {
            proxy_pass http://burrito_auth:10003/auth/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /comments/ {
            proxy_pass http://burrito_comments:10004/comments/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /iofiles/ {
            proxy_pass http://burrito_iofiles:10005/iofiles/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /meta/ {
            proxy_pass http://burrito_meta:10006/meta/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /profile/ {
            proxy_pass http://burrito_profile:10008/profile/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /registration/ {
            proxy_pass http://burrito_registration:10009/registration/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }


        location  /tickets/ {
            proxy_pass http://burrito_tickets:10010/tickets/;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }
    }
}